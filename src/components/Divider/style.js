import styled from 'styled-components/native';
import theme from '../../theme';

export const StyleDivider = styled.View`
  border-bottom-width: 1px;
  border-bottom-color: ${theme.colors.GREY4};
  margin-vertical: 10px;
`;
